# 要件定義書 v0.1

S3互換ストレージを使った個人向けE2E暗号化バックアップアプリ

---

## 1. プロダクト概要

個人向けのE2E暗号化バックアップアプリ。ユーザーが選んだS3互換ストレージ、もしくは運営が用意したストレージにファイル・写真・書類を安全に保管する。

**信頼の核**：鍵さえあればアプリなしでも復号可能な設計。サブスク失効・アプリ停止・運営消滅でもデータは救出できる。

---

## 2. 配信・プラットフォーム

- iOSアプリ（App Store配信）
- React Native で実装
- 課金は App Store IAP

---

## 3. ストレージモード

| | マネージドモード | BYOモード |
|---|---|---|
| バケット提供 | 運営 | ユーザー指定（S3互換なら何でも） |
| ストレージ料金 | アプリ料金に内包 | ユーザーがプロバイダに直接支払い |
| 無料枠（アカウント合算） | 5GB | 10GB |
| 暗号化 | E2E（運営も復号不可） | E2E（運営も復号不可） |

---

## 4. 課金

- **¥480 / 月 / バケット1つ**
- 課金単位はバケット。複数バケット運用するなら本数分課金。
- 無料枠超過時：**ハード停止**（新規アップロード不可、既存ファイルの閲覧・DLは継続可）
- 課金後はプロバイダ上限まで容量無制限
- 上限到達前に通知（80% / 95%）
- 無料枠はアカウント単位で合算（バケット量産による回避不可）

---

## 5. セキュリティ

- E2E暗号化（ゼロ知識）
- 暗号化方式：AES-256-GCM もしくは XChaCha20-Poly1305
- 鍵導出：Argon2id（パスフレーズ → マスター鍵）
- ファイル名・パス・メタデータも暗号化
- 大ファイルはチャンク分割、各チャンクを独立に認証付き暗号化
- 鍵保管：iOS Keychain
- 回復用ニーモニックの発行＋オフライン保管を強く案内

---

## 6. 復旧保証

- **暗号化フォーマットを公開**
- アプリ非依存の復号CLIツールを別途配布
- サブスク失効・アプリ停止・運営消滅でもデータ救出可能であることを保証
- ストア説明文・LPでもこの点を前面に出す

---

## 7. UI構成（3タブ）

### 📁 フォルダタブ
Androidのファイル管理アプリ風UI。
- ヒエラルキー型のファイル管理
- 並び替え（名前 / サイズ / 日付）
- 複数選択、検索
- アップロードFAB、フォルダ作成、移動、削除

### 🖼 アルバムタブ
iOS写真.app風UI。
- 日付グルーピング（年 / 月 / 日）のグリッド表示
- 写真ライブラリからの自動取り込み（バックグラウンド）
- サムネイルは別途暗号化保存（ローカルキャッシュ＋S3）して二段構えで高速表示

### ⚙️ 設定タブ
- アカウント / サブスク状態
- バケット管理（複数追加・切替）
- 鍵の管理（エクスポート / インポート / 再生成）
- 生体認証ロック、自動ロック時間
- 容量使用状況
- データ全削除（App Store要件）

---

## 8. オンボーディングフロー

```
1. ウェルカム（価値提案：「鍵は自分で持つ」）
2. Sign in with Apple
3. バケット選択
   ├─ マネージド：そのまま進む（5GB無料、超過時IAP）
   └─ 互換S3：接続情報入力 → 接続テスト
4. 鍵生成
   ├─ BIP39 12語ニーモニック表示
   └─ 「書き写した」チェックボックス
5. 写真アクセス権限リクエスト
6. はじめての使い方ツアー
```

---

## 9. MVP機能リスト

- バケット接続設定（エンドポイント / リージョン / アクセスキー）
- ファイル / フォルダのアップロード（再開可能・大容量対応）
- 暗号化済みインデックスでの一覧・検索
- ダウンロード＋復号＋完全性チェック
- 使用量メーター表示（無料枠 / 上限 / 警告）
- 写真ライブラリからの自動取り込み
- 生体認証ロック

---

## 10. 使用量計測

- 暗号化後サイズで計測（請求根拠が明確）
- マネージド：サーバー側で計測
- BYO：クライアント側で計測 → サーバー報告
- アカウント単位で合算

---

## 11. App Store対応事項

- Sign in with Apple 対応
- アプリ内アカウント削除導線
- IAPで¥480/月の課金フロー（小規模事業者枠なら手数料15%）

---

## 12. 技術スタック・実装上の論点

### スタック
- React Native
- 暗号化：`react-native-quick-crypto` または libsodium のネイティブバインディング
- バックグラウンドアップロード：ネイティブモジュール（NSURLSession Background）または `react-native-background-upload`

### 先に潰すべき技術リスク
- JSでの暗号化はパフォーマンス上不可。ネイティブ実装が必須
- iOSのバックグラウンドアップロード制約
- 大ファイルのストリーミング暗号化（メモリに全部載せられない）

---

## 13. 確定スコープ外（MVPに入れない）

- 双方向同期（ワンウェイ・アップロードのみで開始）
- 他人へのファイル共有機能
- Android版 / macOS版

---

## 14. 未決事項

### 鍵設計（確定）
- 形式：**BIP39 12語ニーモニックのみ**
- スコープ：**アカウントに1つ**の鍵（全バケット共通）
- オンボーディング時の検証：**「書き写した」チェックボックスのみ**
- 鍵ファイルエクスポート：設定タブから後でいつでも可能

### その他
- [ ] 無料トライアル期間の扱い（無料枠が永久無料なので、追加トライアルは不要かも）
- [ ] サムネイル生成・保存の詳細仕様
- [ ] BYO接続情報の保管方法（iOS Keychain前提だが運用詳細）

---

## 改訂履歴

- v0.1 初版（要件ヒアリングからの落とし込み）
- v0.2 鍵設計を確定（BIP39 12語、アカウント1鍵、シンプル検証）
